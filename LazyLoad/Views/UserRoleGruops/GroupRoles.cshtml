@model IEnumerable<LazyLoad.Models.RoleJoinRoleGruop>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserRole.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserRole.Name)
        </td>
        <td>
            @Html.ActionLink("Delete", "GroupRolesDelete", new { UserRoleGruopID = item.UserRoleGruopID, UserRoleID = item.UserRoleID})
        </td>
    </tr>
}

</table>
